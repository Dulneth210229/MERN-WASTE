import React from 'react'
import RequestNav from '../RequestNav/RequestNav'

function RequestServiceMain() {
  return (
    <div>
        <RequestNav/>
      <h1>Request Service Main(view requests categories)</h1>
    </div>
  )
}

export default RequestServiceMain
